image: node:20

stages:
  - pages

pages:
  stage: pages
  script:
    - npm ci
    - npm run build
    - rm -rf public
    - mkdir -p public
    - cp -r dist/* public/
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
